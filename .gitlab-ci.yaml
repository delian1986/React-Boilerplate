image: node:10.19.0

cache:
  paths:
    - node_modules/

before_script:
  - CI=false npm install

pages:
  stage: deploy
  script:
    - npm install
    - CI=false npm run build
    - cp dist/bundle.js public
  artifacts:
    paths:
      - public
  only:
    - master